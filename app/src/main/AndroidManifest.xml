<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sample.poc_google_billing">

    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.POC_google_billing">
        <activity
            android:name="com.example.poc_google_billing.ShoppingCard"
            android:label="@string/shopping_card"
            android:exported="true" />
        <activity
            android:name="com.example.poc_google_billing.Consumable"
            android:exported="true" />
        <activity
            android:name="com.example.poc_google_billing.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>